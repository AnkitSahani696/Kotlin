<?php include 'db.php'; ?>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Crop | Farm Dashboard</title>

  <!-- Bootstrap 5 CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(135deg, #00b09b, #96c93d);
      font-family: 'Poppins', sans-serif;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .card {
      border: none;
      border-radius: 15px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
      background-color: #fff;
      max-width: 450px;
      width: 100%;
    }

    .card-header {
      background: linear-gradient(135deg, #ff9966, #ff5e62);
      color: #fff;
      border-radius: 15px 15px 0 0;
      text-align: center;
      font-weight: 600;
      font-size: 1.3rem;
      padding: 15px;
    }

    .form-label {
      font-weight: 500;
    }

    .btn-custom {
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: #fff;
      font-weight: 500;
      border-radius: 8px;
      transition: 0.3s ease;
      width: 100%;
    }

    .btn-custom:hover {
      background: linear-gradient(135deg, #764ba2, #667eea);
      transform: scale(1.03);
    }

    .message {
      margin-top: 15px;
      text-align: center;
      font-weight: 500;
      color: #fff;
    }
  </style>
</head>
<body>

  <div class="card">
    <div class="card-header">
      🌾 Add New Crop
    </div>
    <div class="card-body">
      <form method="post">
        <div class="mb-3">
          <label class="form-label">Crop Name</label>
          <input type="text" name="name" class="form-control" placeholder="Enter crop name" required>
        </div>

        <div class="mb-3">
          <label class="form-label">Price per kg</label>
          <input type="number" step="0.01" name="price" class="form-control" placeholder="Enter price" required>
        </div>

        <div class="mb-3">
          <label class="form-label">Quantity (kg)</label>
          <input type="number" name="quantity" class="form-control" placeholder="Enter quantity" required>
        </div>

        <button type="submit" name="submit" class="btn btn-custom">Add Crop</button>
      </form>

      <?php
      if (isset($_POST['submit'])) {
          $name = $_POST['name'];
          $price = $_POST['price'];
          $quantity = $_POST['quantity'];

          $sql = "INSERT INTO crops (crop_name, price, quantity) VALUES ('$name', '$price', '$quantity')";
          if ($conn->query($sql)) {
              echo "<div class='message text-success'>✅ Crop added successfully!</div>";
          } else {
              echo "<div class='message text-danger'>❌ Error: " . $conn->error . "</div>";
          }
      }
      ?>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
